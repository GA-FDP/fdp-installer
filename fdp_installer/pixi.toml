[workspace]
name = "fdp_dev"
description = "Fusion Data Platform environment (slim core via fdp-core metapackage)"
channels = ["conda-forge", "ga-fdp"]
platforms = ["linux-64"]

[dependencies]
# The entire core stack, as one blessed, tested set. See GA-FDP/fdp-core.
fdp-core = "*"

[tasks]

[feature.dev.dependencies]
black = "*"

[environments]
default = ["dev"]
